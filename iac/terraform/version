terraform {
    required_version = ">= 0.12"

    required_providers {
        aws = "~> 2.8"
    }

    backend s3 {
        region = "us-east-1"
        key = "terraform"
    }
}

provider aws {
    region = "us-east-1"
    version = "~> 2.8"
}